class Snippet28 {
    static int compute(int a, int b, int c, int d, int e) {
        return (((a + (b * c)) - (d / (e == 0 ? 1 : e))) * ((a - b) + (c - d))) + ((a * (b + c)) - (d * (e + 1)));
    }
}
